# Backend configuration
GOOGLE_MAPS_API_KEY=your-google-key
# Comma-separated list of HTTPS origins allowed to call the API (e.g., GitHub Pages URL)
FRONTEND_ALLOWED_ORIGINS=https://your-user.github.io/vanguarda-cosmica

# Frontend configuration (Vite)
VITE_GOOGLE_MAPS_API_KEY=your-google-key
# Public HTTPS endpoint for the FastAPI backend that the frontend should call
VITE_API_BASE=https://your-backend.example.com
